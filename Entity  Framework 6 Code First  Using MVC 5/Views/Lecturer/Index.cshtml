@model IEnumerable<COMP3618_Midterm.Models.Lecturer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LectureName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LectureName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LecturerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LecturerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LecturerID })
        </td>
    </tr>
}

</table>
